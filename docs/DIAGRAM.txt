```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                     🌐 INTERNET (Cloudflare DDoS/WAF)                       │
│                                                                             │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
                                   │ HTTPS
                                   │
┌──────────────────────────────────▼──────────────────────────────────────────┐
│                                                                             │
│                   🚪 TRAEFIK API GATEWAY (Ingress)                          │
│                      api.mikeodnis.dev                                      │
│                                                                             │
│  Features:                                                                  │
│  • SSL/TLS Termination (Let's Encrypt)                                     │
│  • JWT Validation Middleware                                               │
│  • Per-Route Rate Limiting                                                 │
│  • Service Discovery (Docker Labels)                                       │
│  • Load Balancing                                                          │
│  • Metrics & Monitoring                                                    │
│                                                                             │
└──────┬──────────────────┬───────────────────┬──────────────────────────────┘
       │                  │                   │
       │ /v1/core/*       │ /v1/media/*       │ /v1/links/*
       │                  │                   │
       ▼                  ▼                   ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────────┐
│              │   │              │   │                  │
│  Core API    │   │  Media API   │   │  Hasura GraphQL  │
│  Node.js     │   │     Go       │   │                  │
│  Fastify     │   │     Chi      │   │  /graphql        │
│              │   │              │   │  /console        │
│  • Users     │   │  • Upload    │   │                  │
│  • Auth      │   │  • Presign   │   │  • Links         │
│  • Business  │   │  • Stream    │   │  • Clicks        │
│    Logic     │   │  • R2 Ops    │   │  • Analytics     │
│              │   │              │   │                  │
│  Port: 3000  │   │  Port: 8080  │   │  Port: 8080      │
│              │   │              │   │                  │
└──────┬───────┘   └──────┬───────┘   └────────┬─────────┘
       │                  │                    │
       │                  │                    │
       └──────────────────┴────────────────────┘
                          │
                          │ TCP/SQL
                          │
              ┌───────────▼────────────┐
              │                        │
              │  PostgreSQL Database   │
              │                        │
              │  Schemas:              │
              │  • core (users)        │
              │  • media (assets)      │
              │  • links (links,       │
              │           clicks)      │
              │                        │
              │  Port: 5432            │
              │                        │
              └────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                   ☁️  CLOUDFLARE R2 CDN STORAGE                             │
│                      cdn.mikeodnis.dev                                      │
│                                                                             │
│  • S3-Compatible Object Storage                                            │
│  • Global CDN Distribution                                                 │
│  • Zero Egress Fees                                                        │
│  • Public/Private Buckets                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                   📊 OBSERVABILITY STACK                                    │
│                                                                             │
│  ┌────────────────┐  ┌─────────────────┐  ┌──────────────┐                │
│  │ OpenTelemetry  │  │  Prometheus     │  │ BetterStack  │                │
│  │ (Traces)       │  │  (Metrics)      │  │ (Monitoring) │                │
│  └────────────────┘  └─────────────────┘  └──────────────┘                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

REQUEST FLOW:
═════════════

1. Client → Cloudflare (DDoS protection)
2. Cloudflare → Traefik (SSL termination, JWT validation)
3. Traefik → Service (based on path: /v1/core, /v1/media, /v1/links)
4. Service → Database/R2 (data operations)
5. Service → Client (JSON response)

All services:
• Log structured JSON to stdout
• Export OpenTelemetry traces
• Expose /health endpoints
• Expose /metrics (Prometheus)
• Use connection pooling
• Support horizontal scaling
```
